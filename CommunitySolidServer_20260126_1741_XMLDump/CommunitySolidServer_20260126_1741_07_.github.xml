<?xml version="1.0" encoding="UTF-8"?>
<volume>
  <meta>
    <generated_at>2026-01-26T17:41:03.893283</generated_at>
    <title>FOLDER: .github</title>
    <root_dir>C:\MyCode\Kristals\CommunitySolidServer</root_dir>
    <file_count>13</file_count>
    <total_size_mb>0.019</total_size_mb>
    <home>Index.xml</home>
    <next_volume>CommunitySolidServer_20260126_1741_08_scripts.xml</next_volume>
    <prev_volume>CommunitySolidServer_20260126_1741_06_documentation.xml</prev_volume>
    <prev_title>documentation</prev_title>
    <next_title>scripts</next_title>
    <short_title>.github</short_title>
  </meta>
  <file_index>
    <entry path=".github/.markdownlint-cli2.cjs" kind="source" size="410" lines="18" />
    <entry path=".github/dependabot.yml" kind="source" size="858" lines="33" />
    <entry path=".github/ISSUE_TEMPLATE/bug-report.md" kind="markdown" size="513" lines="18" />
    <entry path=".github/ISSUE_TEMPLATE/config.yml" kind="source" size="217" lines="5" />
    <entry path=".github/ISSUE_TEMPLATE/feature-request.md" kind="markdown" size="393" lines="13" />
    <entry path=".github/PULL_REQUEST_TEMPLATE.md" kind="markdown" size="1188" lines="23" />
    <entry path=".github/workflows/cth-test.yml" kind="source" size="3333" lines="90" />
    <entry path=".github/workflows/docker.yml" kind="source" size="3422" lines="93" />
    <entry path=".github/workflows/main.yml" kind="source" size="1315" lines="40" />
    <entry path=".github/workflows/mkdocs.yml" kind="source" size="2684" lines="77" />
    <entry path=".github/workflows/npm-test.yml" kind="source" size="4222" lines="149" />
    <entry path=".github/workflows/schedule.yml" kind="source" size="338" lines="18" />
    <entry path=".github/workflows/stale.yml" kind="source" size="1015" lines="29" />
  </file_index>
  <files>
    <file path=".github/.markdownlint-cli2.cjs" size="410" lines="18" kind="source">
'use strict';

/* eslint-disable-next-line import/extensions */
const options = require('../.markdownlint-cli2.cjs');

module.exports = {
  globs: [ '**/*.md' ],
  config: {
    // Re-use the base config
    ...options.config,

    // Disable line length due to requirements for issue templates
    MD013: false,

    // Allow first line to not be top level heading
    MD041: false,
  },
};

    </file>
    <file path=".github/dependabot.yml" size="858" lines="33" kind="source">
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: daily
      time: '02:13'
      timezone: Europe/Brussels
    labels:
      - ⚙️ dependencies
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: daily
      time: '03:35'
      timezone: Europe/Brussels
    target-branch: versions/next-major
    ignore:
      # Ignore minor and patch version updates
      - dependency-name: '*'
        update-types: ['version-update:semver-minor', 'version-update:semver-patch']
      # Sticking with Husky 4.x
      - dependency-name: husky
    labels:
      - ⚙️ dependencies
  - package-ecosystem: docker
    directory: /
    schedule:
      interval: daily
      time: '04:22'
      timezone: Europe/Brussels
    labels:
      - ⚙️ dependencies

    </file>
    <file path=".github/ISSUE_TEMPLATE/bug-report.md" size="513" lines="18" kind="markdown">
---
name: "\U0001F41B Bug report"
about: If something is not working as expected or crashes
title: ''
labels: ''
assignees: ''

---

#### Environment

- Server version: *Output of `community-solid-server --version` for a
  global or `npx community-solid-server --version` for a local installation*
- Node.js version: *Output of `node -v`*
- npm version: *Output of `npm -v`*

#### Description
&lt;!-- Please describe the exact problem as clearly as possible. Provide any error messages thrown. --&gt;

    </file>
    <file path=".github/ISSUE_TEMPLATE/config.yml" size="217" lines="5" kind="source">
blank_issues_enabled: false
contact_links:
  - name: ❓ Question
    url: https://github.com/CommunitySolidServer/CommunitySolidServer/discussions/new/choose
    about: A question or discussion about the server

    </file>
    <file path=".github/ISSUE_TEMPLATE/feature-request.md" size="393" lines="13" kind="markdown">
---
name: "➕ Feature request"
about: Suggest an idea for this project
title: ''
labels: ''
assignees: ''

---

#### Feature description

&lt;!--In case you want to start a discussion about an idea, discussions are better suited for this https://github.com/CommunitySolidServer/CommunitySolidServer/discussions --&gt;
&lt;!--A clear and concise description of what you want to happen.--&gt;

    </file>
    <file path=".github/PULL_REQUEST_TEMPLATE.md" size="1188" lines="23" kind="markdown">
#### 📁 Related issues

&lt;!--
Reference any relevant issues here. Closing keywords only have an effect when targeting the main branch. If there are no related issues, you must first create an issue through https://github.com/CommunitySolidServer/CommunitySolidServer/issues/new/choose
--&gt;

#### ✍️ Description

&lt;!-- Describe the relevant changes in this PR. Also add notes that might be relevant for code reviewers. --&gt;

### ✅ PR check list

Before this pull request can be merged, a core maintainer will check whether

* [ ] this PR is labeled with the correct semver label
    * semver.patch: Backwards compatible bug fixes.
    * semver.minor: Backwards compatible feature additions.
    * semver.major: Breaking changes. This includes changing interfaces or configuration behaviour.
* [ ] the correct branch is targeted. Patch updates can target main, other changes should target the latest versions/* branch.
* [ ] the RELEASE_NOTES.md document in case of relevant feature or config changes.
* [ ] any relevant documentation was updated to reflect the changes in this PR.

&lt;!-- Try to check these to the best of your abilities before opening the PR --&gt;

    </file>
    <file path=".github/workflows/cth-test.yml" size="3333" lines="90" kind="source">
name: Conformance test harness

on:
  workflow_call:
    inputs:
      version:
        type: string
        required: false
        default: latest
        description: Version of the Conformance Test Harness that has to be tested against
      branch:
        type: string
        required: false
        description: branch to run tests against (used for matrix strategy)
      ignore_failures:
        type: boolean
        default: false
        description: Return succes even if there are failures?
        required: false
  workflow_dispatch:
    inputs:
      version:
        type: string
        required: false
        default: latest
        description: Version of the Conformance Test Harness that has to be tested against.
      ignore_failures:
        type: boolean
        default: false
        description: Return succes even if there are failures?
        required: false

jobs:
  # Run the Conformance Test Harness and upload output as artifacts to GitHub
  conformance:
    continue-on-error: ${{ inputs.ignore_failures }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Use Node.js 22.x
        uses: actions/setup-node@v6
        with:
          node-version: 22.x
      - name: Check out the project
        uses: actions/checkout@v6.0.2
        with:
          ref: ${{ inputs.branch || github.ref }}
      - name: Install dependencies and run build scripts
        run: npm ci
      - name: Start the server in the background
        run: npm start &gt; server-output.log  &amp;
      - name: Create the necessary folders
        run: mkdir -p reports/css
      - name: Pull the conformance harness docker
        run: 'docker pull solidproject/conformance-test-harness:${{ inputs.version }}'
      - name: Wait until the server has started
        run: &gt;
          curl --output /dev/null --silent --head --fail --retry 30
          --retry-connrefused --retry-delay 0 -k http://localhost:3000/
      - name: Create users
        run: npx ts-node test/deploy/createAccountCredentials.ts http://localhost:3000/ &gt;&gt; test/deploy/conformance.env
      - name: Run the test harness
        run: &gt;
          docker run -i --rm
          -v "$(pwd)"/reports/css:/reports
          --env-file=./test/deploy/conformance.env
          --network="host"
          solidproject/conformance-test-harness:${{ inputs.version }}
          --skip-teardown
          --output=/reports
          --target=https://github.com/solid/conformance-test-harness/css
      # Steps below use `always()` to make sure logs get uploaded in case the CTH errors
      - name: Sanitize branch name for artifact upload
        id: sanitize
        if: always()
        uses: yeouchien/sanitize-branch-name-action@v1
        with:
          branch-name: ${{ inputs.branch || github.head_ref }}
      - name: Save the reports
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: ${{ steps.sanitize.outputs.sanitized-branch-name }} reports
          path: reports
      - name: Save the server output
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: ${{ steps.sanitize.outputs.sanitized-branch-name }} server output
          path: server-output.log

    </file>
    <file path=".github/workflows/docker.yml" size="3422" lines="93" kind="source">
name: Docker

on:
  workflow_call:
    secrets:
      DOCKERHUB_TOKEN:
        required: true
      DOCKERHUB_USERNAME:
        required: true

jobs:

  docker-meta:
    # Generates the metadata (labels and tags) for the docker containers
    #   - push to main results in the edge tag
    #   - version tag results in edge, latest and semver, major, major.minor tags
    #   - push to versions/ results in the next tag
    runs-on: ubuntu-latest
    outputs:
      labels: ${{ steps.meta-main.outputs.labels || steps.meta-version.outputs.labels }}
      tags: ${{ steps.meta-main.outputs.tags || steps.meta-version.outputs.tags }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2
      - if: startsWith(github.ref, 'refs/tags/v') || (github.ref == 'refs/heads/main')
        name: Docker meta edge and version tag
        id: meta-main
        uses: docker/metadata-action@v5
        with:
          images: |
            solidproject/community-server
          # Edge will always be executed (without latest tag), semver only on tag push events (with latest tag)
          tags: |
            type=edge
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
          github-token: ${{ secrets.github_token }}
      - if: startsWith(github.ref, 'refs/heads/versions/')
        name: Docker meta next
        id: meta-version
        uses: docker/metadata-action@v5
        with:
          images: |
            solidproject/community-server
          # Just one label: next (no latest here) for the last pushed commit on this branch
          tags: |
            type=raw,value=next
          github-token: ${{ secrets.github_token }}

  docker:
    # Builds, tests and pushes docker containers
    needs: docker-meta
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and export to docker
        uses: docker/build-push-action@v6
        with:
          context: .
          load: true
          tags: ${{ needs.docker-meta.outputs.tags }}
          labels: ${{ needs.docker-meta.outputs.labels }}

      - name: Test all docker-built image tags
        shell: bash
        # Loop over all generated image:tag names and docker run them.
        # If they aren't built previously, the command will error.
        # If they don't work, the job will fail.
        # If they work the --version argument should return a version and the job succeeds.
        run: |
          while read i; do
            docker run --rm --pull never $i --version
          done &lt;&lt;&lt; "${{ needs.docker-meta.outputs.tags }}";

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          platforms: linux/amd64,linux/arm/v7,linux/arm/v8,linux/arm64/v8
          tags: ${{ needs.docker-meta.outputs.tags }}
          labels: ${{ needs.docker-meta.outputs.labels }}

    </file>
    <file path=".github/workflows/main.yml" size="1315" lines="40" kind="source">
name: CI
on:
  push:
    branches:
      - main
      - 'versions/*'
    tags:
      - 'v*'
  pull_request:

concurrency:
  group: ${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  npm-test:
    # Run the tests in every case
    uses: ./.github/workflows/npm-test.yml

  cth-test:
    # Run the Conformance Test Harness on PRs targeting main or versions/
    if: github.event_name == 'pull_request' &amp;&amp; ( github.base_ref == 'main' || startsWith( github.base_ref, 'versions/' ) )
    uses: ./.github/workflows/cth-test.yml
    with:
      ignore_failures: true
      version: 1.1.14 # The latest version that CSS is confirmed to pass

  docker:
    # Build docker containers on version tag, push to main and push to versions/
    needs: npm-test
    if: startsWith(github.ref, 'refs/tags/v') || (github.ref == 'refs/heads/main') || startsWith(github.ref, 'refs/heads/versions/')
    uses: ./.github/workflows/docker.yml
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

  mkdocs-release:
    # Release documentation on version tag, but ignore pre-releases
    if: startsWith(github.ref, 'refs/tags/v') &amp;&amp; !contains(github.ref, '-')
    uses: ./.github/workflows/mkdocs.yml

    </file>
    <file path=".github/workflows/mkdocs.yml" size="2684" lines="77" kind="source">
name: Documentation
on:
  workflow_call:

  # Additional trigger to deploy changes to the documentation/ folder
  #   on push to main, ignoring tags so we don't trigger twice upon release
  push:
    branches:
      - main
    paths:
      - documentation/**
    tags-ignore:
      - '*'

jobs:
  mkdocs-prep:
    # Runs the markdown linter to ensure we don't release faulty markdown.
    # Also gets the correct major version, whether the job is triggered by a version tag
    #   or a push to main to update the latest documentation.
    runs-on: ubuntu-latest
    outputs:
      major: ${{ steps.tagged_version.outputs.major || steps.current_version.outputs.major }}
    steps:
      - uses: actions/checkout@v6.0.2
      - uses: actions/setup-node@v6
        with:
          node-version: 16.x
      - run: npm ci --ignore-scripts
      - name: Lint documentation markdown
        run: npm run lint:markdown -- documentation/**/*.md
      - if: startsWith(github.ref, 'refs/tags/v')
        name: Get tagged version
        id: tagged_version
        uses: battila7/get-version-action@v2
      - if: github.ref == 'refs/heads/main'
        name: Get current version
        id: current_version
        run: |
          VERSION=$(git show origin/main:package.json | jq -r .version | grep -Po '^(\d+)')
          echo "major=$VERSION" &gt;&gt; $GITHUB_OUTPUT

  mkdocs:
    runs-on: ubuntu-latest
    needs: mkdocs-prep
    steps:
      - uses: actions/checkout@v6.0.2
      - uses: actions/setup-python@v6
        with:
          python-version: 3.x
      - run: pip install mkdocs-material
      - run: pip install mike
      - run: git config user.name ci-bot
      - run: git config user.email ci-bot@example.com
      - run: git fetch origin gh-pages --depth=1
      - run: |
          cd documentation &amp;&amp; mike deploy --push --update-aliases \
            ${{ needs.mkdocs-prep.outputs.major }}.x latest

  typedocs:
    # Build typedocs and publish them to the GH page.
    # `mike deploy` overwrites the entire folder for a version so these need to be (re)built afterwards.
    needs: [mkdocs-prep, mkdocs]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2
      - uses: actions/setup-node@v6
        with:
          node-version: 16.x
      - run: npm ci --ignore-scripts
      - name: Generate typedocs
        run: npm run typedocs
      - name: Deploy typedocs
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs
          destination_dir: ${{ needs.mkdocs-prep.outputs.major }}.x/docs

    </file>
    <file path=".github/workflows/npm-test.yml" size="4222" lines="149" kind="source">
name: NPM test

on:
  workflow_call:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2
      - uses: actions/setup-node@v6
        with:
          node-version: 20.x
      - run: npm ci --ignore-scripts
      - run: npm run lint

  test-unit:
    # Run unit tests on windows and linux
    runs-on: ${{ matrix.operating-system }}
    strategy:
      matrix:
        operating-system:
          - ubuntu-latest
          - windows-latest
        node-version:
          - '18.0'
          - 18.x
          - '20.0'
          - 20.x
          - '22.1'
          - 22.x
          - '24.0'
          - 24.x
    timeout-minutes: 15
    steps:
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
      - name: Ensure line endings are consistent
        run: git config --global core.autocrlf input
      - name: Check out repository
        uses: actions/checkout@v6.0.2
      - name: Install dependencies and run build scripts
        run: npm ci
      - name: Type-check tests
        run: npm run test:ts
      - name: Run unit tests
        run: npm run test:unit
      - name: Submit unit test coverage
        uses: coverallsapp/github-action@master
        with:
          github-token: ${{ secrets.github_token }}
          flag-name: test-unit-${{ matrix.node-version }}-${{ matrix.operating-system }}
          parallel: true

  test-integration:
    # Run integration tests on linux (full suite, with external dependencies)
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version:
          - 18.x
          - 20.x
          - 22.x
          - 24.x
    env:
      TEST_DOCKER: true
    services:
      sparql-endpoint:
        image: tenforce/virtuoso
        env:
          SPARQL_UPDATE: true
        ports:
          - 4000:8890
      redis:
        image: redis
        ports:
          - 6379:6379
    timeout-minutes: 20
    steps:
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
      - name: Check out repository
        uses: actions/checkout@v6.0.2
      - name: Install dependencies and run build scripts
        run: npm ci
      - name: Run integration tests
        run: npm run test:integration

  test-integration-windows:
    # Run integration tests on windows (no external dependencies)
    runs-on: windows-latest
    strategy:
      matrix:
        node-version:
          - 18.x
          - 20.x
          - 22.x
          - 24.x
    timeout-minutes: 20
    steps:
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
      - name: Ensure line endings are consistent
        run: git config --global core.autocrlf input
      - name: Check out repository
        uses: actions/checkout@v6.0.2
      - name: Install dependencies and run build scripts
        run: npm ci
      - name: Run integration tests
        run: npm run test:integration

  test-configs:
    # Test startup of CSS with all configs inside the config/ folder
    runs-on: ubuntu-latest
    services:
      sparql-endpoint:
        image: tenforce/virtuoso
        env:
          SPARQL_UPDATE: true
        ports:
          - 4000:8890
    steps:
      - name: Use Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20.x
      - name: Check out repository
        uses: actions/checkout@v6.0.2
      - name: Install dependencies and run build scripts
        run: npm ci
      - name: Run deploy tests
        run: npm run test:deploy

  coveralls:
    # Consolidate test coverage reports from all unit tests
    needs: test-unit
    runs-on: ubuntu-latest
    steps:
      - name: Consolidate test coverage from different jobs
        uses: coverallsapp/github-action@master
        with:
          github-token: ${{ secrets.github_token }}
          parallel-finished: true

    </file>
    <file path=".github/workflows/schedule.yml" size="338" lines="18" kind="source">
name: Conformance test harness

on:
  schedule:
    - cron: '23 5 * * *'
  workflow_dispatch:

jobs:
  conformance:
    strategy:
      fail-fast: false
      matrix:
        branch:
          - main
          - versions/next-major
    uses: ./.github/workflows/cth-test.yml
    with:
      branch: ${{ matrix.branch }}

    </file>
    <file path=".github/workflows/stale.yml" size="1015" lines="29" kind="source">
name: Stale issues and PRs
on:
  schedule:
    - cron: '30 1 * * *'

permissions:
  issues: write

jobs:
  stale:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/stale@v10
        with:
          debug-only: true
          stale-issue-label: 🏚️ abandoned
          stale-issue-message: &gt;
            This issue has been automatically marked as `🏚️ abandoned`
            because it has not had recent activity. It will be closed if no
            further activity occurs.
          close-issue-message: &gt;
            Closing stale issue. If this issue is still relevant, please reopen it.
          days-before-issue-stale: 60
          days-before-close: 30
          days-before-pr-stale: -1
          operations-per-run: 700
          exempt-issue-labels: '🐛 bug,☀️ enhancement,📚 documentation,➕ feature,🐌 performance,
            ➕ test,📝 task,:ant: worker threads,👩🏾‍💻 developer experience'
          exempt-all-assignees: true

    </file>
  </files>
</volume>
